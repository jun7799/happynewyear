# LeanCloud 云引擎配置文件

# 运行时环境
runtime: python-3.9

# 安装依赖
install: pip install -r requirements.txt

# 启动命令
# LeanCloud 会自动设置 PORT 环境变量
# 我们用 gunicorn 启动，监听 LEANENGINE_PORT 端口
start: gunicorn -w 2 -b 0.0.0.0:$LEANENGINE_PORT app:app

# 健康检查路径（可选）
healthCheckPath: /health
