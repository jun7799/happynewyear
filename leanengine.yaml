# LeanCloud 云引擎配置文件

# 安装依赖
install: pip install -r requirements.txt

# 启动命令（非 WSGI 方式）
# LeanCloud 会自动设置 LEANCLOUD_APP_PORT 环境变量
run: gunicorn -w 2 -b 0.0.0.0:$LEANCLOUD_APP_PORT app:app

# 健康检查路径
healthCheckPath: /health
